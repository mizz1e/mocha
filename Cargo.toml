[package]
name = "mocha"
version = "0.0.0"
edition = "2021"

[dependencies]
rustix = { version = "0.38.34", default-features = false, features = ["libc", "mount", "process", "std", "system", "thread"] }
